services:
  # The Node.js Backend Web Service
  - type: web
    name: rental-server
    env: node
    region: ohio
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: FIREBASE_KEY
        sync: false
      - key: STRIPE_SECRET
        sync: false
